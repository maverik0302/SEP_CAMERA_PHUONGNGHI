@model IEnumerable<SEP_CAMERA_PHUONGNGHI.Models.OrderDetail>

@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/Admin/Views/Shared/_mLayoutAdmin.cshtml";
}

<h2>Details</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.price_product)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.num)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.total_price_product)
        </th>
       
        <th>
            @Html.DisplayNameFor(model => model.Oder.order_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbProduct.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.price_product)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.num)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.total_price_product)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.Oder.order_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbProduct.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.detail_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.detail_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.detail_id })
        </td>
    </tr>
}

</table>
